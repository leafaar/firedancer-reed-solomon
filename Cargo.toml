[package]
name = "firedancer-reed-solomon"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Reed-Solomon erasure coding using Firedancer's optimized implementation"
repository = "https://github.com/firedancer-io/firedancer"
keywords = ["reed-solomon", "erasure", "fec", "solana", "firedancer"]
categories = ["algorithms", "cryptography"]

[lib]
name = "fd_reedsol"
path = "src/lib.rs"

[dependencies]
thiserror = "1.0"

[build-dependencies]
cc = "1.0"
